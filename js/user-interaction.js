"use strict";
//
// // TODO: Ask the user for their name.
// //       Keep asking if an empty input is provided.
var response;
do {
    response = prompt('What is your name?');
}while(response == "");
alert('hello, ' + response);
//
// // TODO: Show an alert message that welcomes the user based on their input.

var message;

message = prompt('Would you prefer to view this page in unicorn font?')
    if(message  ) {
    alert('Excellent!');
    } else {
    alert ('Your lose!');
    }

// // TODO: Ask the user if they like pizza.
// //       Based on their answer show a creative alert message.
//
var response = confirm ('Do you like pizza?');
    if(response ) {
    alert ('Cool, me too!');
    }
    else {
        alert ('Oh, we can\'t be friends.');
    }
//
// function bigEvent() {}
//
//     var registered;
//
//     registered = confirm('Are you registered?');
//     if (registered) {
//
//     }

(function() {



}) ();